/// Constants used in the package
class Constants {
  /// 默认字体文件路径
  static const String defaultFontAssetsPath = "assets/fonts/";

  /// 默认图标文件写入路径
  static const String defaultWritePath = "lib/icons/";

  /// 默认 pubspec.yaml 文件路径
  static const String pubspecConfig = "pubspec.yaml";
}
